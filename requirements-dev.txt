# Development dependencies for ESP32 Temperature Sensor project

# Code formatting and linting
ruff>=0.16.4
black>=26.5.1

# Type checking
mypy>=2.3.0
types-pyyaml
types-pyserial

# Testing
pytest-playwright>=0.4.0
hypothesis>=6.0  # Property-based tests (tests/test_power_hypothesis.py, test_geometry_hypothesis.py)
fastapi>=0.141.1  # Imported by device_manager tests (tests/test_device_manager_async.py)
# Needed by tests/test_device_manager_async.py (@pytest.mark.asyncio); those
# tests only run where the device-manager deps (fastapi etc.) are installed.
pytest-asyncio>=1.0

# Code coverage (optional)
pytest-cov>=4.0
